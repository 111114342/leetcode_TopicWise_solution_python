class Solution:
    def buildArray(self, target: List[int], n: int) -> List[str]:
        l=[]
        for i in range(1,n+1):
            if i in target:
                l.append("Push")
            else:
                l.append("Push")
                l.append("Pop")
            if i==target[-1]:
                return l
                
                
        
